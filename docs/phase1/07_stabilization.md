# Phase 1.7: 안정화 기간

**목표**: 4주간 모니터링 및 안정성 확보

**소요 기간**: 4주

**담당**: 전체 팀

---

## 1. 안정화 기간 개요

### 1.1 목표

- **안정성**: 크래시율 < 0.1%, 에러율 < 0.1%
- **성능**: 평균 API 응답 시간 < 500ms
- **사용자 만족도**: > 4.0/5.0
- **마이그레이션 완료율**: > 95%
- **비용**: Supabase Pro Plan 범위 내

### 1.2 주간 목표

| 주차 | 주요 활동 | 목표 |
|------|----------|------|
| **1주차** | 집중 모니터링, 크리티컬 버그 수정 | 100% 롤아웃 달성 |
| **2주차** | 성능 최적화, 사용자 피드백 반영 | 성능 지표 목표 달성 |
| **3주차** | 비용 최적화, 마이그레이션 독려 | 마이그레이션 90% 달성 |
| **4주차** | 최종 검증, Isar 제거 준비 | 마이그레이션 95% 달성 |

---

## 2. 1주차: 집중 모니터링

### 2.1 일일 체크리스트

**매일 오전 10시 (팀 회의)**:
- [ ] Supabase Dashboard 지표 확인
  - API 요청 수
  - 에러율
  - 평균 응답 시간
  - Database 사용량

- [ ] Firebase Analytics 확인
  - DAU (Daily Active Users)
  - 마이그레이션 성공률
  - 크래시율
  - Feature Flag 상태

- [ ] Firebase Crashlytics 확인
  - 신규 크래시 0건 목표
  - 기존 크래시 추적 및 수정

- [ ] 사용자 피드백 확인
  - 앱 스토어 리뷰
  - 이메일 문의
  - 커뮤니티 피드백

**매일 오후 6시 (데일리 리포트)**:
- [ ] 주요 지표 리포트 작성
- [ ] 발견된 이슈 트래킹
- [ ] 다음 날 작업 계획

### 2.2 크리티컬 버그 대응

**P0 버그** (즉시 수정):
- 앱 크래시
- 데이터 손실
- 로그인 불가
- 마이그레이션 실패 (반복적)

**P1 버그** (24시간 내 수정):
- 주요 기능 동작 불가
- 성능 저하 (응답 시간 > 2초)
- UI 깨짐

**P2 버그** (1주 내 수정):
- 부분적 기능 오류
- UI 개선 사항

**버그 수정 프로세스**:
1. 버그 리포트 접수 (GitHub Issues)
2. 심각도 분류 (P0/P1/P2)
3. 재현 확인
4. 수정
5. 테스트
6. 핫픽스 배포 (P0/P1)
7. 사용자 알림

### 2.3 핫픽스 배포

**핫픽스가 필요한 경우**:
- P0 버그 발견
- 보안 취약점 발견
- 데이터 무결성 위협

**핫픽스 절차**:
1. 버그 수정 (별도 브랜치 `hotfix/v1.1.1`)
2. 긴급 테스트 (1-2시간)
3. 버전 업데이트 (`1.1.0` → `1.1.1`)
4. iOS/Android 빌드
5. 앱 스토어 긴급 심사 요청
6. Remote Config로 임시 우회 (가능한 경우)
7. 배포 완료 후 사용자 공지

---

## 3. 2주차: 성능 최적화

### 3.1 성능 지표 분석

**Supabase Dashboard**:
- API 응답 시간 분포 확인
- 느린 쿼리 식별 (> 1초)
- Database Index 최적화

**느린 쿼리 예시**:
```sql
-- Supabase Dashboard > Database > Query Performance

-- 느린 쿼리 식별
SELECT
  query,
  calls,
  mean_exec_time,
  total_exec_time
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 10;
```

### 3.2 쿼리 최적화

**Before (느림)**:
```dart
// 모든 컬럼 조회
final response = await _supabase
    .from('weight_logs')
    .select('*')
    .eq('user_id', userId)
    .order('log_date', ascending: false);
```

**After (빠름)**:
```dart
// 필요한 컬럼만 조회
final response = await _supabase
    .from('weight_logs')
    .select('id, log_date, weight_kg')
    .eq('user_id', userId)
    .order('log_date', ascending: false)
    .limit(30); // 최근 30개만
```

### 3.3 인덱스 추가

**느린 쿼리 분석 후 인덱스 추가**:
```sql
-- symptom_logs에서 user_id + log_date 조회가 느림
CREATE INDEX idx_symptom_logs_user_date_severity
ON symptom_logs(user_id, log_date DESC, severity);

-- dose_records에서 dosage_plan_id + administered_at 조회가 느림
CREATE INDEX idx_dose_records_plan_administered_completed
ON dose_records(dosage_plan_id, administered_at DESC)
WHERE is_completed = true;
```

### 3.4 캐싱 전략

**Riverpod 캐싱 활용**:
```dart
// 자주 조회되는 데이터 캐싱 (1시간)
@Riverpod(keepAlive: true)
Future<List<BadgeDefinition>> badgeDefinitions(BadgeDefinitionsRef ref) async {
  final repository = ref.watch(badgeRepositoryProvider);

  // 1시간 후 자동 갱신
  ref.cacheFor(const Duration(hours: 1));

  return await repository.getBadgeDefinitions();
}
```

### 3.5 성능 개선 체크리스트

- [ ] 느린 쿼리 식별 및 최적화
- [ ] 필요한 인덱스 추가
- [ ] 불필요한 `select('*')` 제거
- [ ] 페이지네이션 구현 (limit/offset)
- [ ] 캐싱 전략 적용
- [ ] 이미지 최적화 (압축, lazy loading)

---

## 4. 3주차: 비용 최적화

### 4.1 Supabase 비용 모니터링

**Supabase Dashboard > Settings > Billing**:
- Database 사용량: X GB / 8 GB
- Bandwidth: X GB / 50 GB
- Edge Functions: X invocations / 500K
- Storage: X GB / 100 GB

**목표**: Pro Plan ($25/월) 범위 내

### 4.2 비용 절감 전략

**1. Database 쿼리 최적화**:
- 불필요한 JOIN 제거
- Pagination 적용 (전체 조회 방지)
- 중복 쿼리 제거 (캐싱)

**2. Bandwidth 절감**:
- 대용량 데이터 압축
- 이미지 CDN 사용 (Supabase Storage)
- 불필요한 데이터 전송 방지

**3. Edge Functions 최적화**:
- 불필요한 호출 제거
- 배치 처리 (여러 요청을 하나로)
- 캐싱 활용

**4. Storage 정리**:
- 미사용 파일 삭제
- 이미지 압축

### 4.3 마이그레이션 독려

**마이그레이션 미완료 사용자 대상**:
- 앱 내 알림 (홈 화면 배너)
- 푸시 알림 (주 1회)
- 이메일 (마이그레이션 안내)

**알림 문구**:
```
📦 클라우드 백업을 완료하세요!

모든 기기에서 데이터를 안전하게 이용하려면
클라우드 백업이 필요합니다.

[지금 백업하기]
```

**인센티브 제공**:
- 마이그레이션 완료 시 뱃지 획득
- 또는 프리미엄 기능 1주일 무료

---

## 5. 4주차: 최종 검증

### 5.1 마이그레이션 완료율 확인

**목표**: 95% 이상

**확인 방법**:
```sql
SELECT
  COUNT(*) AS total_users,
  COUNT(migration_completed_at) AS migrated_users,
  ROUND(COUNT(migration_completed_at)::NUMERIC / COUNT(*)::NUMERIC * 100, 2) AS migration_rate
FROM users;
```

**95% 미달 시**:
- 미완료 사용자에게 개별 연락
- 마이그레이션 지원 제공
- 강제 마이그레이션 검토 (최후의 수단)

### 5.2 데이터 무결성 검증

**검증 항목**:
1. 레코드 수 일치 (Isar vs Supabase)
2. 주요 필드 값 일치
3. 관계 무결성 (FK)
4. 날짜/시간 정확성

**검증 스크립트**:
```dart
Future<void> validateDataIntegrity(String userId) async {
  // 1. Weight logs 검증
  final isarCount = await _isar.weightLogDtos
      .filter()
      .userIdEqualTo(userId)
      .count();

  final supabaseCount = await _supabase
      .from('weight_logs')
      .select('id', count: CountOption.exact)
      .eq('user_id', userId)
      .count();

  if (isarCount != supabaseCount) {
    throw Exception('Weight logs count mismatch: Isar=$isarCount, Supabase=$supabaseCount');
  }

  // 2. Symptom logs 검증
  // ...

  print('Data integrity check passed for user $userId');
}
```

### 5.3 성능 벤치마크

**최종 성능 확인**:
```dart
final stopwatch = Stopwatch()..start();

// 1. 체중 기록 100개 조회
await repository.getWeightLogs(userId);
final p1 = stopwatch.elapsedMilliseconds;

// 2. 증상 기록 100개 조회
await repository.getSymptomLogs(userId);
final p2 = stopwatch.elapsedMilliseconds - p1;

// 3. 투여 기록 100개 조회
await repository.getDoseRecords(planId);
final p3 = stopwatch.elapsedMilliseconds - p1 - p2;

stopwatch.stop();

print('Performance: WeightLogs=${p1}ms, SymptomLogs=${p2}ms, DoseRecords=${p3}ms');
```

**목표**: 각 쿼리 < 300ms

### 5.4 Isar 제거 준비

**체크리스트**:
- [ ] 마이그레이션 완료율 ≥ 95%
- [ ] 4주간 안정적 운영 (크래시율 < 0.1%)
- [ ] 데이터 무결성 100% 검증
- [ ] 성능 지표 목표 달성
- [ ] 사용자 만족도 > 4.0/5.0
- [ ] 롤백 필요 없음 확인

**모두 달성 시 → Phase 1.8 Isar 제거로 진행**

---

## 6. 주간 리포트

### 6.1 리포트 템플릿

**파일 위치**: `docs/phase1/reports/week_X_report.md`

```markdown
# Phase 1.7 안정화 - X주차 리포트

**기간**: YYYY-MM-DD ~ YYYY-MM-DD

## 주요 지표

| 지표 | 목표 | 실제 | 상태 |
|------|------|------|------|
| 크래시율 | < 0.1% | 0.05% | ✅ |
| 에러율 | < 0.1% | 0.08% | ✅ |
| 평균 API 응답 시간 | < 500ms | 420ms | ✅ |
| 마이그레이션 완료율 | > 90% | 92% | ✅ |
| DAU | - | 8,500 | - |
| 사용자 만족도 | > 4.0 | 4.2 | ✅ |

## 발견된 이슈

### P0 (크리티컬)
- 없음

### P1 (높음)
- [#123] 투여 기록 중복 저장 (수정 완료)

### P2 (중간)
- [#124] 체중 차트 애니메이션 버벅임 (진행 중)
- [#125] 알림 시간 설정 UI 개선 (백로그)

## 성능 개선

- `weight_logs` 쿼리 최적화: 800ms → 350ms
- `symptom_logs` 인덱스 추가: 1200ms → 400ms

## 비용

- Supabase: $18 / $25 (72%)
- Firebase: $5 / $10 (50%)

## 다음 주 계획

- [ ] P2 이슈 수정
- [ ] 마이그레이션 95% 목표 달성
- [ ] 성능 추가 최적화

## 팀 피드백

- 전반적으로 안정적
- 사용자 피드백 긍정적
```

### 6.2 주간 회의 (월요일 오전)

**아젠다**:
1. 지난 주 리포트 리뷰
2. 주요 이슈 논의
3. 이번 주 목표 설정
4. 작업 할당

---

## 7. 사용자 커뮤니케이션

### 7.1 공지사항

**앱 내 공지** (주 1회):
```
📢 Phase 1 안정화 진행 중

현재 클라우드 동기화 기능이 안정적으로 운영되고 있습니다.
불편사항이 있으시면 언제든지 문의해주세요.

- 마이그레이션 완료율: 92%
- 평균 응답 속도: 420ms
- 크래시율: 0.05%

감사합니다!
```

### 7.2 사용자 설문조사

**Google Forms / Typeform**:

**질문**:
1. 클라우드 동기화 기능에 만족하십니까? (1-5점)
2. 마이그레이션 과정이 쉬웠습니까? (1-5점)
3. 앱 성능이 개선되었습니까? (1-5점)
4. 개선이 필요한 부분이 있습니까? (자유 응답)
5. 추천 의향이 있습니까? (NPS)

**인센티브**: 설문 참여자 중 추첨 10명에게 기프티콘

---

## 8. 안정화 완료 조건

### 8.1 필수 조건 (모두 충족)

- [ ] 4주간 운영 완료
- [ ] 크래시율 < 0.1% (4주 평균)
- [ ] 에러율 < 0.1% (4주 평균)
- [ ] 평균 API 응답 시간 < 500ms (4주 평균)
- [ ] 마이그레이션 완료율 ≥ 95%
- [ ] 사용자 만족도 > 4.0/5.0
- [ ] P0/P1 이슈 0건
- [ ] Supabase 비용 Pro Plan 범위 내
- [ ] 데이터 무결성 100% 검증

### 8.2 선택 조건 (권장)

- [ ] 앱 스토어 평점 ≥ 4.5
- [ ] 사용자 이탈률 < 5%
- [ ] DAU 증가 추세
- [ ] 사용자 피드백 긍정적

---

## 9. 다음 단계

✅ Phase 1.7 완료 후:
- **[Phase 1.8: Isar 완전 제거](./08_cleanup.md)** 문서로 이동하세요.

---

## 트러블슈팅

### 문제 1: 마이그레이션 완료율 정체
**증상**: 4주차에도 90% 미달
**해결**:
- 미완료 사용자 분석 (왜 안하는지)
- 개별 연락 및 지원
- 마이그레이션 프로세스 개선

### 문제 2: 비용 증가
**증상**: Supabase 비용이 Pro Plan 초과
**해결**:
- 비용 분석 (어디서 많이 사용하는지)
- 쿼리 최적화
- 불필요한 Edge Function 호출 제거
- 필요 시 Custom Plan 검토

### 문제 3: 성능 저하
**증상**: API 응답 시간 증가
**해결**:
- 느린 쿼리 식별 및 최적화
- Database connection pool 확인
- Supabase 리전 확인
- 필요 시 Read Replica 추가 검토
