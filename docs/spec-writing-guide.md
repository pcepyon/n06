# AI 에이전트를 위한 구현 문서 작성 가이드

> 이 문서는 Claude, Cursor 등 AI 코딩 에이전트에게 구현을 지시할 때
> 최적의 결과를 얻기 위한 문서 작성 원칙을 정리한다.

---

## 핵심 원칙

### 1. 컨텍스트 > 스펙

```
❌ 상세한 구현 스펙 512줄
✅ 정확한 컨텍스트 50줄 + 에이전트 탐색
```

**이유**: 토큰이 많아질수록 모델의 "attention budget"이 소진되어 핵심을 놓친다 (Context Rot).

### 2. 참조 > 하드코딩

```
❌ "DemoWeightScreen을 이렇게 만들어라" + 100줄 코드 예시
✅ "기존 WeightRecordScreen 패턴을 따라 데모 버전 확장"
```

**이유**: 에이전트가 실제 코드를 탐색하면 현재 패턴과 100% 일치하는 코드를 생성한다.

### 3. What & Why > How

```
❌ Provider 구현 코드, ASCII UI 레이아웃
✅ 달성 목표, 사용자 시나리오, 제약 조건
```

**이유**: How는 에이전트가 코드베이스를 보고 결정하는 게 더 정확하다.

---

## 문서 구조

### 필수 섹션 (유지)

| 섹션 | 내용 | 분량 |
|------|------|------|
| **개요** | 한 문장 목표 | 1-2줄 |
| **배경/문제** | 왜 이게 필요한가 | 3-5줄 |
| **목표** | 달성해야 할 것 (측정 가능) | bullet 3-5개 |
| **사용자 시나리오** | 핵심 플로우 설명 | 시나리오당 5줄 이내 |
| **제약 조건** | 반드시 지켜야 할 것 | bullet 형태 |
| **참조 코드** | 따라야 할 기존 패턴 경로 | 파일 경로 목록 |
| **성공 지표** | 완료 조건 | 체크리스트 |

### 삭제/축소 섹션 (Anti-patterns)

| 섹션 | 문제 | 대안 |
|------|------|------|
| UI ASCII Art | 실제 디자인 시스템과 불일치 | "기존 X 컴포넌트 스타일 따름" |
| 상세 코드 예시 | 기존 패턴과 충돌 가능 | 참조 파일 경로만 제공 |
| 가상 파일 구조 | 실제와 다를 수 있음 | "기존 feature 구조 따름" |
| 상세 Provider 구현 | 현재 패턴과 맞지 않을 수 있음 | state-management.md 참조 지시 |

---

## 템플릿

```markdown
# {기능명} 구현 스펙

## 개요
{한 문장으로 이 기능이 무엇인지}

## 배경
- 현재 문제: {무엇이 부족한가}
- 기대 효과: {이걸 만들면 어떻게 나아지는가}

## 목표
- [ ] {구체적이고 측정 가능한 목표 1}
- [ ] {구체적이고 측정 가능한 목표 2}
- [ ] {구체적이고 측정 가능한 목표 3}

## 사용자 시나리오

### 시나리오 1: {시나리오 이름}
1. 사용자가 {행동}
2. 시스템이 {반응}
3. 결과: {기대 상태}

### 시나리오 2: {시나리오 이름}
...

## 제약 조건
- {반드시 지켜야 할 규칙}
- {기술적 제한사항}
- {비즈니스 규칙}

## 참조 코드
> 에이전트는 아래 파일을 먼저 읽고 패턴을 따를 것

| 참조 대상 | 경로 | 참조 이유 |
|----------|------|----------|
| {비슷한 기능} | `lib/features/X/...` | 구조 패턴 |
| {UI 컴포넌트} | `lib/shared/widgets/...` | 디자인 시스템 |
| {상태 관리} | `lib/features/X/application/...` | Provider 패턴 |

## 성공 지표
- [ ] {동작 확인 기준 1}
- [ ] {동작 확인 기준 2}
- [ ] 기존 테스트 통과
- [ ] 빌드 성공
```

---

## 좋은 예 vs 나쁜 예

### 개요

```markdown
# ❌ 나쁜 예
데모 모드는 비로그인 사용자가 실제 앱 UI를 더미 데이터로 체험할 수 있는
모드이다. 기존 게스트홈의 "정보 전달" 역할을 "체험 유도"로 전환하여,
자연스러운 회원가입 전환을 유도한다. 핵심 원칙은 실제 앱과 동일한 UI
사용, 데모 표시 배너로 체험 모드임을 명확히 인지, 체험 중 자연스러운
로그인 유도 시점 설계, 게스트홈과 온보딩의 역할 분리이다...

# ✅ 좋은 예
비로그인 사용자가 더미 데이터로 실제 앱을 체험하여 회원가입 전환율을 높인다.
```

### 참조 코드

```markdown
# ❌ 나쁜 예
class DemoSessionNotifier extends _$DemoSessionNotifier {
  @override
  DemoSessionState build() => DemoSessionState.initial();

  void addWeightLog(double weight) {
    state = state.copyWith(
      userWeightLogs: [...state.userWeightLogs,
        WeightLog(date: DateTime.now(), weight: weight)]
    );
  }
  // ... 50줄 더
}

# ✅ 좋은 예
## 참조 코드
| 참조 대상 | 경로 |
|----------|------|
| 세션 상태 관리 | `lib/features/auth/application/auth_notifier.dart` |
| 체중 기록 패턴 | `lib/features/weight/application/weight_notifier.dart` |
```

### UI 설명

```markdown
# ❌ 나쁜 예
┌─────────────────────────────────────────┐
│ 🎮 체험 모드                        [X] │
│ 실제 기록은 로그인 후 저장돼요           │
└─────────────────────────────────────────┘

**스타일**:
- 배경: `AppColors.infoBackground` (파란색 계열)
- 아이콘: 🎮 또는 Material icon `sports_esports`
- 텍스트: `AppTypography.labelMedium`
- 높이: 48px (SafeArea 포함)

# ✅ 좋은 예
## 데모 배너
- 화면 상단에 "체험 모드" 안내 배너 표시
- 닫기 가능, 화면 이동 시 다시 표시
- 기존 InfoBanner 컴포넌트 스타일 참조: `lib/shared/widgets/info_banner.dart`
```

---

## 컨텍스트 양 가이드라인

| 기능 복잡도 | 권장 분량 | 예시 |
|------------|----------|------|
| 단순 (버그 수정, 소규모 추가) | 10-30줄 | 버튼 추가, 텍스트 변경 |
| 중간 (새 화면, 기능 확장) | 50-100줄 | 새 화면 1-2개, API 연동 |
| 복잡 (새 feature 모듈) | 100-200줄 | 새 feature 폴더 전체 |

**200줄 초과 시**: 문서를 분할하거나, Phase별로 나누어 지시

---

## 에이전트 지시 패턴

### 탐색 먼저, 구현 나중

```markdown
## 구현 전 확인사항
1. `lib/features/weight/` 구조 파악
2. `WeightNotifier` 상태 관리 패턴 확인
3. 기존 위젯 재사용 가능 여부 판단
4. 위 분석 후 구현 시작
```

### 점진적 구현

```markdown
## 구현 순서
1. 핵심 기능만 먼저 구현 (MVP)
2. 동작 확인 후 세부 기능 추가
3. 엣지 케이스 처리
```

### 질문 유도

```markdown
## 결정 필요 사항
- 데모 데이터 저장: 로컬 vs SharedPreferences?
- 로그인 유도 타이밍: 행동 기반 vs 시간 기반?

→ 에이전트가 코드베이스 확인 후 추천하도록 유도
```

---

## 체크리스트

문서 작성 완료 전 확인:

- [ ] 개요가 2문장 이내인가?
- [ ] 코드 예시 대신 참조 경로를 제공했는가?
- [ ] ASCII UI 다이어그램을 삭제했는가?
- [ ] 목표가 측정 가능한가?
- [ ] 200줄을 초과하지 않는가?
- [ ] 에이전트가 탐색해야 할 파일을 명시했는가?

---

## 참고 자료

- [Anthropic: Effective Context Engineering](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [DigitalOcean: Context Management for AI Agents](https://docs.digitalocean.com/products/gradient-ai-platform/concepts/context-management/)
- [Potpie: How to Write Good Prompts for LLM Code Generation](https://github.com/potpie-ai/potpie/wiki/How-to-write-good-prompts-for-generating-code-from-LLMs)
